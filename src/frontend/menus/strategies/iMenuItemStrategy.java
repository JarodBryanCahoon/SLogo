package frontend.menus.strategies;

/**
 * An interface that defines a single execute method for all menu item buttons to call upon click
 * @author Albert
 *
 */
public interface iMenuItemStrategy {
	/**
	 * executes the overriden commands upon clicking the associated menu button
	 */
	void execute();
}
